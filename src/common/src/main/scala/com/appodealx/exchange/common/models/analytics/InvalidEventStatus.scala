package com.appodealx.exchange.common.models.analytics

object InvalidEventStatus {

  val EXPIRED = "expired"
  val INVALID_TOKEN = "invalid_token"
  val DUPLICATE = "duplicate"
  val NO_CONTEXT_AND_TOKEN = "no_content_and_token"
  val NO_CONTEXT = "no_context"
  val NO_TOKEN = "no_token"

}
